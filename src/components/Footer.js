import React from "react"

import * as styles from "../styles/components/Footer.module.scss"

export default function Footer() {
    return (
        <footer className={"wrapper " + styles.footer}>
            <p>Designed and built by Marcello Sebastian</p>
        </footer>
    )
}
